## Main Toggle

Use this Setting to enable or disable the entire Progress Counter feature.

```ruby 
ProgressCounter = true 
```

### Possible Values

- `true` = enabled: all available Progress Counter Tracking features can be used (according to the individual settings below).
- `false` = disabled: Turns off this feature entirely. **Other settings below will have no effect.**
