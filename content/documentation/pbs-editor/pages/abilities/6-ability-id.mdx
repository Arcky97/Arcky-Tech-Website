## ID

### Overview

The ID is the internal identifier used by the script to refer to an Ability.  
Every Ability must have a unique ID.
<br/>
Typically, this ID matches the Ability's name, but written in **all capital letters**, without spaces or special characters.  
The editor automatically filters out invalid characters to prevent issues.
<br/>
In the PBS file, the ID is what defines the start of a new entry, and it must appear first in each Ability block.  
However, the editor handles this formatting for you when saving.
<br/>
---

### Default Value

The ID is a required field and has no default value.  
If the ID is not defined—or if an Ability with the same ID already exists—the editor will prevent you from saving the entry.

ID
Overview

The ID serves as the <Gr>internal identifier</Gr> used by the engine to reference an Ability. Each Ability must have a <Rd>unique ID</Rd> to function correctly.

<br/>

In most cases, the ID mirrors the Ability’s name, formatted in <Yl>ALL CAPS</Yl> and without spaces or special characters (e.g. OVERGROW, LEVITATE).
To help prevent errors, the editor will automatically <Gr>filter out invalid characters</Gr> as you type.

<br/>

Within the PBS file, the ID defines the <Gr>start of a new Ability entry</Gr> and must always appear first in each block. This structure is handled automatically by the editor when saving, so no manual formatting is required.

<br/>