## File Selection

This dropdown menu lists all PBS files related to Abilities.
<br/>
The main file is typically named `abilities.txt`, but additional files such as `abilities_xxx.txt` can also be used.  
The part after the underscore (`xxx`) may be any text, but the underscore itself is required — without it, the editor will not recognize the file as an Ability PBS file.
<br/>
If only a single file is available, the dropdown will be disabled, and that file will be selected automatically.

File Selection

This dropdown allows you to choose between all available <Gr>Ability PBS files</Gr> detected in your project.

<br/>

The primary file is typically named abilities.txt, but the editor also supports additional files following the format abilities_xxx.txt.

<br/>

The <Yl>underscore (_)</Yl> is required for these extra files to be recognized. Any text can follow it (e.g. abilities_custom.txt, abilities_modded.txt), but files without this naming pattern will <Rd>not be detected</Rd> as Ability PBS files.

<br/>

If only a single file is available, the dropdown will be automatically <Gr>disabled</Gr>, and that file will be selected by default.