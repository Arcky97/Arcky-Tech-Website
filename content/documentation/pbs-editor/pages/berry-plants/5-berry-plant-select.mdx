## Berry Plant Selection

This dropdown menu wich lists all Berry Plants entries by their ID for the currently selected file.
<br/>
In your PBS file, these IDs are written in the format `[xxx]`, but the editor displays them without the brackets for readability.
The brackets must still be present in the actual PBS file-when saving, the editor automatically re-add them even though they are not shown in the interface.