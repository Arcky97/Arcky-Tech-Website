## Berry Plant Overwrite

This toggle determines how the editor handles saving the current Berry Plant entry:

- **Off** — The editor will create a **new Berry Plant entry**, but only if the ID does not already exist in a PBS file.
- **On** — The editor will **overwrite the existing Berry Plant** that matches the current ID in the PBS file.